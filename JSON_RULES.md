# JSON_RULES.md

Whenever JSON is generated:

Ensure valid syntax.

Rules:

double quotes only
no trailing commas
correct nesting
valid arrays
no comments inside JSON

Example:

{
 "title": "News",
 "items": []
}